# Test Suite

Vitest test suites for the Kanban app. Some tests are skipped until environment is configured.

## Install test deps
```bash
npm i -D vitest @testing-library/react @testing-library/jest-dom jsdom fast-check supertest
```

## Run tests
```bash
npm run test
# or
npx vitest --run
```
